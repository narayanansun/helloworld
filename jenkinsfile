pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Add your build steps here
                // For example, use PyInstaller to package your Python app
                python helloworld.py'
            }
        }
    }
}
